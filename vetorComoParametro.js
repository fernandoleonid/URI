const btnTeste = document.getElementById('teste');
btnTeste.addEventListener('click',msg => {

const A = [10000,5000,2000,1000,500,200,100,50,25,10,5,1];

function msg (y,x){
    for (i=0; i<x.length;i++){
        alert (y + " -- " + x[i]);
    }
}

msg ("fernando",A);

} );